spring.application.name=user-service
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
server.port=8081

# Usado por Client.PublicationClient para resolver las publicaciones del
# vendedor en GET/PUT /users/me. Mismo servicio que consulta el BFF (ver
# bff-pcmarketbuilder/src/config.ts, PUBLICATIONS_BASE_URL).
publications.base-url=${PUBLICATIONS_BASE_URL:http://localhost:8083/api/v1/publications}
